# Crawler runtime — kept in sync with ../crawler/requirements.txt.
# (FAB auth provider is installed in the Dockerfile via Airflow's constraints
# file so its version always matches the Airflow build.)
selenium==4.43.0
beautifulsoup4==4.12.3
lxml==5.3.0
httpx[http2]==0.27.2
tenacity==9.0.0
